There is also no git history to fall back on: this folder ignores itself (`.gitignore` contains
   `*`), which is the default. If you need a copy of a standard before you change it, keep one
   outside this folder.
